<footer class="site-footer">
  <div class="site-footer__border"></div>
  <div class="site-footer__inner">
    <div class="site-footer__info">
      <p class="site-footer__brand">Gamification</p>
      <p class="site-footer__text">
        Base de connaissances pour comprendre, concevoir et impl&eacute;menter
        des syst&egrave;mes de gamification efficaces et &eacute;thiques.
      </p>
    </div>

    <nav class="site-footer__nav" aria-label="Liens utiles">
      <a href="/comprendre/">Comprendre</a>
      <a href="/concevoir/">Concevoir</a>
      <a href="/creer/">Cr&eacute;er</a>
      <a href="/valider/">Valider</a>
      <a href="/humain/">L'humain</a>
      <a href="/outils/">Outils</a>
    </nav>

    <div class="site-footer__bottom">
      <p class="site-footer__copy">&copy; 2026 Gamification. Tous droits r&eacute;serv&eacute;s.</p>
      <p class="site-footer__credits">
        Construit avec &#10084;&#65039; et <a href="https://astro.build" target="_blank" rel="noopener noreferrer">Astro</a>
      </p>
    </div>
  </div>
</footer>

<style>
  .site-footer {
    position: relative;
    background: var(--glass-bg);
    backdrop-filter: var(--glass-blur);
    -webkit-backdrop-filter: var(--glass-blur);
    padding: var(--space-12) var(--space-6) var(--space-8);
    margin-top: auto;
  }

  .site-footer__border {
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 1px;
    background: var(--gradient-primary);
    opacity: 0.4;
  }

  .site-footer__inner {
    max-width: var(--content-max-width);
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: var(--space-8);
  }

  .site-footer__brand {
    font-size: var(--text-lg);
    font-weight: 700;
    margin-bottom: var(--space-2);
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .site-footer__text {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    line-height: var(--leading-relaxed);
    max-width: 48ch;
  }

  .site-footer__nav {
    display: flex;
    flex-wrap: wrap;
    gap: var(--space-2) var(--space-5);
  }

  .site-footer__nav a {
    font-size: var(--text-sm);
    color: var(--color-text-muted);
    text-decoration: none;
    transition: color var(--transition-fast), text-shadow var(--transition-fast);
  }

  .site-footer__nav a:hover {
    color: var(--color-link);
    text-shadow: 0 0 12px rgba(124, 58, 237, 0.3);
    text-decoration: none;
  }

  .site-footer__bottom {
    display: flex;
    flex-wrap: wrap;
    justify-content: space-between;
    align-items: center;
    gap: var(--space-2);
    padding-top: var(--space-6);
    border-top: 1px solid var(--color-border);
  }

  .site-footer__copy {
    font-size: var(--text-xs);
    color: var(--color-text-muted);
  }

  .site-footer__credits {
    font-size: var(--text-xs);
    color: var(--color-text-muted);
  }

  .site-footer__credits a {
    color: var(--color-link);
    transition: color var(--transition-fast), text-shadow var(--transition-fast);
  }

  .site-footer__credits a:hover {
    color: var(--color-link-hover);
    text-shadow: 0 0 10px rgba(124, 58, 237, 0.3);
    text-decoration: none;
  }
</style>
